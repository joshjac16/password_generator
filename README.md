# password_generator

Generates random passwords with user inputs
This is a simple javascript code that generates strong unique passwords
for a user.
The user gets to decide what characters are contained in the password and
also the length of the password
